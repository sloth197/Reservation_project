<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper      
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"      
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="reserv">
    <insert id="register" parameterType="SignupVo">
        insert into name(name, id, pw, email) values(#{name}, #{id}, #{pw}, #{email});
    </insert>
    <select id=""></select>